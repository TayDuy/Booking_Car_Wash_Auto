# System Architecture

High-level architecture: Client (React) <-> REST API (Spring Boot) <-> PostgreSQL (Supabase). Components: Auth, Booking, Loyalty, Promotions, Reporting. Use load balancer, stateless services, and managed DB. Use Azure/AWS managed services for production.

Include diagrams in assets/diagrams.
