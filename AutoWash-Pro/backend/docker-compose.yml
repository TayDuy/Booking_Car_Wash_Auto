services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autowash-backend
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - autowash-net

networks:
  autowash-net:
    driver: bridge
