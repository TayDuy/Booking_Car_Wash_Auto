spring.application.name=backend
app.jwt.secret=${JWT_SECRET:ThisIsAVerySecretKeyForJWTAuthenticationInAutoWashProBackend123!}
app.jwt.expiration-ms=86400000

# Server port (Render sẽ gán port qua biến môi trường)
server.port=${PORT:8080}

# PostgreSQL Configuration (Supabase)
spring.datasource.url=${DB_URL:jdbc:postgresql://db.psrtylewnuevdtvhrbnh.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Autowash@gr4}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
